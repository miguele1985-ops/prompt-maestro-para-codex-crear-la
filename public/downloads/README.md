# Sustituir APK

Coloca aqui el archivo real:

`modo-crisis-survival.apk`

No se incluye un APK falso. Antes de publicar actualiza `src/content/site-config.ts` y `src/content/downloads.ts` con version, tamano, fecha y SHA-256 real.
